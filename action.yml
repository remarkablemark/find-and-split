name: split-files
description: Split files with GitHub Actions.
author: remarkablemark
inputs:
  version:
    description: Description
    default: 1.2.3
runs:
  using: composite
  steps:
    - name: Print version
      shell: bash
      run: |
        echo 'version: ${{ inputs.version }}'
branding:
  icon: activity
  color: white
